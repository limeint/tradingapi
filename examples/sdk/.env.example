# Copy this file to .env, replace the placeholders, then export it before
# running an example:
#
#   cp examples/sdk/.env.example examples/sdk/.env
#   set -a
#   source examples/sdk/.env
#   set +a
#
# .env is gitignored. Never commit a real secret.
TRADE_API_SECRET=tapi_sk_replace_me
TRADE_API_SYMBOL=AAPL@XNGS
TRADE_API_QUANTITY=1
