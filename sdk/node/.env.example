# Copy to .env, replace the placeholders, then export the variables before
# running an example. Never commit real secrets or account identifiers.
TRADE_API_SECRET=tapi_sk_replace_me
TRADE_API_ACCOUNT_ID=1234567
TRADE_API_SYMBOL=SBER@MISX
