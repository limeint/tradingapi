# Copy to .env, replace the placeholders, then export the variables with
# `set -a; source .env; set +a`. The SDK does not load .env automatically.
# Never commit real secrets.
TRADE_API_SECRET=tapi_sk_replace_me
TRADE_API_SYMBOL=AAPL@XNAS
