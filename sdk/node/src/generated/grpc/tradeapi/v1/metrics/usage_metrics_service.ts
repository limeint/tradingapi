// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: grpc/tradeapi/v1/metrics/usage_metrics_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../../google/protobuf/timestamp.js";

export const protobufPackage = "grpc.tradeapi.v1.metrics";

/** Request to get current usage metrics */
export interface GetUsageMetricsRequest {
}

/** Information about current usage metrics */
export interface GetUsageMetricsResponse {
  /** List of current quotas and their use. */
  quotas: GetUsageMetricsResponse_QuotaUsage[];
}

/** Quota */
export interface GetUsageMetricsResponse_QuotaUsage {
  /** Method name */
  name: string;
  /** The total limit for this quota. */
  limit: bigint;
  /** How many available units are left in the current window. */
  remaining: bigint;
  /** The time when the quota counter will be reset (start of a new window). */
  resetTime: Date | undefined;
}

function createBaseGetUsageMetricsRequest(): GetUsageMetricsRequest {
  return {};
}

export const GetUsageMetricsRequest: MessageFns<GetUsageMetricsRequest> = {
  encode(_: GetUsageMetricsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsageMetricsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsageMetricsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetUsageMetricsRequest {
    return {};
  },

  toJSON(_: GetUsageMetricsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetUsageMetricsRequest>): GetUsageMetricsRequest {
    return GetUsageMetricsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetUsageMetricsRequest>): GetUsageMetricsRequest {
    const message = createBaseGetUsageMetricsRequest();
    return message;
  },
};

function createBaseGetUsageMetricsResponse(): GetUsageMetricsResponse {
  return { quotas: [] };
}

export const GetUsageMetricsResponse: MessageFns<GetUsageMetricsResponse> = {
  encode(message: GetUsageMetricsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.quotas) {
      GetUsageMetricsResponse_QuotaUsage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsageMetricsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsageMetricsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quotas.push(GetUsageMetricsResponse_QuotaUsage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUsageMetricsResponse {
    return {
      quotas: globalThis.Array.isArray(object?.quotas)
        ? object.quotas.map((e: any) => GetUsageMetricsResponse_QuotaUsage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUsageMetricsResponse): unknown {
    const obj: any = {};
    if (message.quotas?.length) {
      obj.quotas = message.quotas.map((e) => GetUsageMetricsResponse_QuotaUsage.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetUsageMetricsResponse>): GetUsageMetricsResponse {
    return GetUsageMetricsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUsageMetricsResponse>): GetUsageMetricsResponse {
    const message = createBaseGetUsageMetricsResponse();
    message.quotas = object.quotas?.map((e) => GetUsageMetricsResponse_QuotaUsage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetUsageMetricsResponse_QuotaUsage(): GetUsageMetricsResponse_QuotaUsage {
  return { name: "", limit: 0n, remaining: 0n, resetTime: undefined };
}

export const GetUsageMetricsResponse_QuotaUsage: MessageFns<GetUsageMetricsResponse_QuotaUsage> = {
  encode(message: GetUsageMetricsResponse_QuotaUsage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.limit !== 0n) {
      if (BigInt.asIntN(64, message.limit) !== message.limit) {
        throw new globalThis.Error("value provided for field message.limit of type int64 too large");
      }
      writer.uint32(16).int64(message.limit);
    }
    if (message.remaining !== 0n) {
      if (BigInt.asIntN(64, message.remaining) !== message.remaining) {
        throw new globalThis.Error("value provided for field message.remaining of type int64 too large");
      }
      writer.uint32(24).int64(message.remaining);
    }
    if (message.resetTime !== undefined) {
      Timestamp.encode(toTimestamp(message.resetTime), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsageMetricsResponse_QuotaUsage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsageMetricsResponse_QuotaUsage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.remaining = reader.int64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.resetTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUsageMetricsResponse_QuotaUsage {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      limit: isSet(object.limit) ? BigInt(object.limit) : 0n,
      remaining: isSet(object.remaining) ? BigInt(object.remaining) : 0n,
      resetTime: isSet(object.resetTime)
        ? fromJsonTimestamp(object.resetTime)
        : isSet(object.reset_time)
        ? fromJsonTimestamp(object.reset_time)
        : undefined,
    };
  },

  toJSON(message: GetUsageMetricsResponse_QuotaUsage): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.limit !== 0n) {
      obj.limit = message.limit.toString();
    }
    if (message.remaining !== 0n) {
      obj.remaining = message.remaining.toString();
    }
    if (message.resetTime !== undefined) {
      obj.resetTime = message.resetTime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<GetUsageMetricsResponse_QuotaUsage>): GetUsageMetricsResponse_QuotaUsage {
    return GetUsageMetricsResponse_QuotaUsage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUsageMetricsResponse_QuotaUsage>): GetUsageMetricsResponse_QuotaUsage {
    const message = createBaseGetUsageMetricsResponse_QuotaUsage();
    message.name = object.name ?? "";
    message.limit = (object.limit !== undefined && object.limit !== null) ? BigInt(object.limit) : 0n;
    message.remaining = (object.remaining !== undefined && object.remaining !== null) ? BigInt(object.remaining) : 0n;
    message.resetTime = object.resetTime ?? undefined;
    return message;
  },
};

/**
 * Service that provides Trade API usage metrics.
 * Allows clients to track their activity, including information about current quotas and limits,
 * to avoid exceeding established limits.
 */
export type UsageMetricsServiceDefinition = typeof UsageMetricsServiceDefinition;
export const UsageMetricsServiceDefinition = {
  name: "UsageMetricsService",
  fullName: "grpc.tradeapi.v1.metrics.UsageMetricsService",
  methods: {
    /**
     * Get current usage metrics for a user
     * Example HTTP request:
     * GET /v1/usage
     * Authorization: <token>
     */
    getUsageMetrics: {
      name: "GetUsageMetrics",
      requestType: GetUsageMetricsRequest as typeof GetUsageMetricsRequest,
      requestStream: false,
      responseType: GetUsageMetricsResponse as typeof GetUsageMetricsResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          8338: [
            new Uint8Array([
              21,
              98,
              19,
              10,
              17,
              10,
              13,
              65,
              117,
              116,
              104,
              111,
              114,
              105,
              122,
              97,
              116,
              105,
              111,
              110,
              18,
              0,
            ]) as Uint8Array,
          ],
          578365826: [new Uint8Array([11, 18, 9, 47, 118, 49, 47, 117, 115, 97, 103, 101]) as Uint8Array],
        },
      },
    },
  },
} as const;

export interface UsageMetricsServiceImplementation<CallContextExt = {}> {
  /**
   * Get current usage metrics for a user
   * Example HTTP request:
   * GET /v1/usage
   * Authorization: <token>
   */
  getUsageMetrics(
    request: GetUsageMetricsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetUsageMetricsResponse>>;
}

export interface UsageMetricsServiceClient<CallOptionsExt = {}> {
  /**
   * Get current usage metrics for a user
   * Example HTTP request:
   * GET /v1/usage
   * Authorization: <token>
   */
  getUsageMetrics(
    request: DeepPartial<GetUsageMetricsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetUsageMetricsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
